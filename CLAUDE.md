# Skills 仓库规则

## 仓库定位

本仓库保存独立 Skill 的可审查快照和对应项目文档。不同 Skill 必须使用独立目录，不得把其他项目的代码、配置或未提交改动混入当前任务。

## 目录约定

- 可运行 Skill 放在 `skills/<name>/`
- 项目规则与交接材料放在 `projects/<name>/`
- 每个项目必须有 `README.md`、`CLAUDE.md`、`CONTEXT.md` 和 `lessons.md`
- `AGENTS.md` 必须指向同目录下的 `CLAUDE.md`
- 临时文件不进入仓库，统一留在本地项目的 `scratch/`

## 数据边界

- 不提交原始课程、付费资料、知识星球数据库、音视频、原始转写和附件
- 不提交 `.env`、Token、Cookie、私钥、账号信息、运行日志、缓存和虚拟环境
- 可以提交当前正式 Skill 随附的派生证据层，包括页级 OCR、短证据摘录、来源定位、质量状态和聚合统计
- 派生证据必须保留 `ocr_unverified`、`asr_unverified` 等质量标签，不能包装成人工校对原文
- Private 仓库不等于可以忽略最小披露原则

## 修改规则

- 先读目标项目的 `projects/<name>/CLAUDE.md`、`CONTEXT.md` 和 `lessons.md`
- 只修改本次任务直接涉及的项目
- 用户纠正需求后，先更新项目 `lessons.md`
- 阶段完成后更新项目 `CONTEXT.md`
- 不把计划写成已完成，不把索引覆盖写成深度蒸馏
- 不擅自创建分支、worktree、标签或改写 Git 历史
- 暂存时只列出明确文件，禁止用 `git add .` 或 `git add -A`

## Web 总控边界

网页版 GPT 只能读取 GitHub 快照，不能看到本机 Skills、memory、原始语料、进程、软链接和远端节点。

网页版 GPT 给出的工单属于上层规划。执行前，本地 Codex 必须重新核对实际路径、文件、测试、工具版本和同步状态，并据此修改工单。任何写有已验证、已存在或可直接运行的断言，都必须有 GitHub 文件证据，或者明确标为需要本地核验。

## 验收原则

- 每项验收必须可观察、可判定并有证据位置
- 不使用优化一下、更加完善、基本可用等模糊标准
- 区分仓库内验收、本机验收和多节点验收
- 科研判断不得把相关性升级为因果，不得把创作者材料当作科学事实
- 所有未完成项必须显式保留
