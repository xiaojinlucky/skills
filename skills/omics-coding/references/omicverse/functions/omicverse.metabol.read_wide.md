# omicverse.metabol.read_wide #

- Package: omicverse
- Language: Python
- Function: `omicverse.metabol.read_wide`
- Source: https://omicverse.readthedocs.io/en/latest/api/reference/omicverse.metabol.read_wide.html
- Fetched at: 2026-08-03T14:16:23+00:00

## Summary

Load a generic wide (samples × metabolites) table into AnnData.

## Signature

```text
omicverse.metabol. read_wide ( path , * , sep = '\\t' , sample_col = None , group_col = None )
```

## Parameters

- `path`: ( str | Path )
- `sep`: ( str (default: '\\t' ))
- `sample_col`: ( Optional [ str ] (default: None ))
- `group_col`: ( Optional [ str ] (default: None ))

## Full Documentation

# omicverse.metabol.read_wide #

omicverse.metabol. read_wide ( path , * , sep = '\\t' , sample_col = None , group_col = None ) [source] #

Load a generic wide (samples × metabolites) table into AnnData.

Parameters :

-
path ( `str `| `Path `)

-
sep ( `str `(default: `'\\t' `))

-
sample_col ( `Optional `[ `str `] (default: `None `))

-
group_col ( `Optional `[ `str `] (default: `None `))
