# omicverse.pl.ForbiddenCity #

- Package: omicverse
- Language: Python
- Function: `omicverse.pl.ForbiddenCity`
- Source: https://omicverse.readthedocs.io/en/latest/api/reference/omicverse.pl.ForbiddenCity.html
- Fetched at: 2026-08-03T14:16:23+00:00

## Summary

Forbidden City traditional-color palette utility.

## Signature

```text
class omicverse.pl. ForbiddenCity
```

## Parameters

- `None`: No description detected.

## Full Documentation

# omicverse.pl.ForbiddenCity #

class omicverse.pl. ForbiddenCity [source] #

Forbidden City traditional-color palette utility.

Parameters :

None

Returns :

Initializes internal color lookup tables and grouped palettes.

Return type :

None

__init__ ( ) [source] #

Methods

`__init__ `()

`get_cmap `(color_range[, return_cmap])

`get_cmap_seg `(colors[, N])

`get_color `(name)

`get_color_rgb `(name)

`visual_color `([loc_range, num_per_row])

Attributes

`blue `

`blue1 `

`blue2 `

`blue3 `

`blue4 `

`brown `

`brown1 `

`brown2 `

`brown3 `

`brown4 `

`green `

`green1 `

`green2 `

`green3 `

`grey `

`grey1 `

`grey2 `

`pink `

`pink1 `

`pink2 `

`purple `

`purple1 `

`purple2 `

`red `

`red1 `

`red2 `

`yellow `

`yellow1 `

`yellow2 `

`yellow3 `
