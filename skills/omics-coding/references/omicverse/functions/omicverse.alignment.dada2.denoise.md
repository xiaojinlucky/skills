# omicverse.alignment.dada2.denoise #

- Package: omicverse
- Language: Python
- Function: `omicverse.alignment.dada2.denoise`
- Source: https://omicverse.readthedocs.io/en/latest/api/reference/omicverse.alignment.dada2.denoise.html
- Fetched at: 2026-08-03T14:16:23+00:00

## Summary

Dispatch to pydada2.dada (the divisive denoiser).

## Signature

```text
omicverse.alignment.dada2. denoise ( derep , err = None , ** kwargs )
```

## Parameters

- `err`: ( Optional [ ndarray ] (default: None ))
- `derep`: Detected from function signature; no parameter description detected.
- `**kwargs`: Detected from function signature; no parameter description detected.

## Full Documentation

# omicverse.alignment.dada2.denoise #

omicverse.alignment.dada2. denoise ( derep , err = None , ** kwargs ) [source] #

Dispatch to `pydada2.dada `(the divisive denoiser).

Parameters :

err ( `Optional `[ `ndarray `] (default: `None `))
