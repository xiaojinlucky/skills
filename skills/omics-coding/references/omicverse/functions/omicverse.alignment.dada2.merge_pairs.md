# omicverse.alignment.dada2.merge_pairs #

- Package: omicverse
- Language: Python
- Function: `omicverse.alignment.dada2.merge_pairs`
- Source: https://omicverse.readthedocs.io/en/latest/api/reference/omicverse.alignment.dada2.merge_pairs.html
- Fetched at: 2026-08-03T14:16:23+00:00

## Summary

min_overlap ( int (default: 12 ))

## Signature

```text
omicverse.alignment.dada2. merge_pairs ( dadaF , derepF , dadaR , derepR , min_overlap = 12 , max_mismatch = 0 )
```

## Parameters

- `min_overlap`: ( int (default: 12 ))
- `max_mismatch`: ( int (default: 0 ))
- `dadaF`: Detected from function signature; no parameter description detected.
- `derepF`: Detected from function signature; no parameter description detected.
- `dadaR`: Detected from function signature; no parameter description detected.
- `derepR`: Detected from function signature; no parameter description detected.

## Full Documentation

# omicverse.alignment.dada2.merge_pairs #

omicverse.alignment.dada2. merge_pairs ( dadaF , derepF , dadaR , derepR , min_overlap = 12 , max_mismatch = 0 ) [source] #

Parameters :

-
min_overlap ( `int `(default: `12 `))

-
max_mismatch ( `int `(default: `0 `))
